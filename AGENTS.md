# 仓库约定

这是一个 Agent Skills 仓库，改编自 [mattpocock/skills](https://github.com/mattpocock/skills)。

目标读者是**别的领域的人**：设计师、写作者、老师、做生意的、研究别的学科的。他不写代码，也不打算学。他带进来的是**创意、想法和对自己领域的判断**，代码是你的活。

所有技能都必须服务于一件事：**把他的想法做成能用的东西，同时让他看懂发生了什么、并且始终由他掌舵。**

## 结构

```
skills/<skill-name>/
├── SKILL.md              必需，带 YAML frontmatter
├── agents/openai.yaml    可选，Codex 的 UI 元数据
└── <其他>.md             可选，该技能自己的参考文档
```

`openai.yaml` 是可选的：现有技能都带着，新技能不写也行。别为一个你不用的工具背上义务。

技能目录是扁平的，没有分桶。每个技能都必须同时出现在：

- `.claude-plugin/plugin.json` 的 `skills` 数组
- `README.md` 的技能一览表（技能名链到它的 `SKILL.md`）
- `skills/learn-start/SKILL.md` 的路由表和技能一览表

新增、改名、删除技能时三处都要同步。**一个不提新技能、或者还路由到已删技能的入口，是一个会说谎的路由。**

## 写技能的规矩

这套技能的读者不写代码，他来是为了把想法做出来，不是来上课的。每个 `SKILL.md` 都要落实这四条，并在必要时明说给用户听：

1. **活你干完，创意和方向问他。** 代码、命令、配置、修复、技术选型，你全部动手做到底，不留空位、不留作业、不让他「练一下」。只有三类事归用户：
   - **只有他能做的**：注册账号、点网页上的授权按钮、输密码、付钱、亲眼验收。
   - **只有他能提供的**：内容、文案、名字、审美偏好、他领域里的规矩。**这些不许你替他编。** 需要而他还没给，就问他要，或者填一个**明确标着「这是占位」**的东西并在收尾时再提醒一次。悄悄用自己编的内容交付，是把他的项目变成你的作品。
   - **只有他能定的**：做给谁、先做哪个、值不值得为它花钱或多花几天。给 2 到 3 个选项，每个写清好处和代价，标出你推荐哪个，然后**停下来等他拍板**。

   反过来说：**纯技术的取舍不要丢给他。** 用哪个框架、数据存哪种格式、CSS 怎么组织，他没有判断依据，问了只会得到一句「你决定」或者盲选第一个，那是假的参与。这类你自己定，然后用一句话交待你定了什么、为什么、以后想换难不难。分界线很简单：**这个问题他能用自己领域的语言回答吗？** 「你平时在哪写东西」他能答；「Markdown 存 Git 还是接 CMS」他不能。要问的是前者，后者由你从前者的答案里推出来。
2. **术语当场翻译。** 任何黑话第一次出现就配一句白话解释，不要等用户问。这包括你自己刚敲出来的东西：命令名、文件名、报错里的词、你新建的那个函数叫什么意思。讲不清的调用 Skill 工具 "jargon-buster"。
3. **一次只推进一件事。** 一个问题、一个任务、一个改动、一条建议。用户的挫败感几乎都来自一次给太多。
4. **每跑一步，跟一句解释。** 每执行一个动作（跑命令、改文件、装依赖），紧接着用一到两句话说清干了什么、为什么、结果说明什么。不攒到最后一次性汇报，不只贴命令输出让用户自己看。**技能之间是隔离的，所以这条规则要写进每一个会执行动作的 `SKILL.md`，不能只写在一处。**

第 1 条是这套技能最容易写错的地方，说清楚它的边界：**「让他看懂」不等于「让他动手」。** 讲清楚你为什么这么改、改完他去哪看效果，是必须做的；把核心那几行空着让他填，是不许做的。他要是自己主动说想试着写，那就让他写，你在旁边看着；但这是他开口要的，不是你安排的作业。

其他约定：

- **手动触发的技能不能被任何其他技能调用。** 这是硬规则，不是风格偏好。`disable-model-invocation: true` 的技能只有人能启动，别的技能连用 Skill 工具点它的名字都不行。跨技能依赖只能写成让人手动敲命令：「输入 `/jackwangskills:learn-tdd`，我带你补一个测试再回来」，然后**停下来等**。
- **指路必须写完整命令 `/jackwangskills:<技能名>`。** 插件安装的技能命令带插件名前缀（`<plugin.json 的 name>:<技能名>`），少了前缀敲下去没反应。**代价要知道：改插件名会让全仓库 30 处指路一起失效**，改名时全局搜 `jackwangskills:` 一起换。
- **自动触发的技能之间，依赖写成「调用 Skill 工具 "<name>"」**，不要写 `/skill` 也不要跨目录引用 `../other-skill/FILE.md`。共享的参考文档放在拥有它的技能目录里，别的技能通过调用该技能来用。
- **给用户看的输出格式要写成代码块模板**，不要只用散文描述。模板是给 agent 照抄的，散文会被自由发挥。
- **散文里不用破折号。** 该用逗号、冒号、句号、括号或连词的地方就用它们，不要做无脑字符替换。
- **frontmatter 的 `description` 同时写中英文触发词。** 中文写给用户看，英文触发短语（`Use when the user…`）提高模型自动命中率。

## 触发方式

| 技能 | 触发 |
| --- | --- |
| `learn-start`、`jargon-buster`、`wait-what` | 自动（model-invoked） |
| `mentor-grill`、`learn-roadmap`、`learn-tdd`、`learn-ship`、`learn-refactor`、`learn-debug` | 手动（`disable-model-invocation: true`） |

分界标准是**接住的自动，接管的手动**：`jargon-buster`、`wait-what`、`learn-start` 只是接住用户、换个说法或指个路，说完他手上的事还在原处；六个技能会接管整个流程（问一轮需求、排一份路线图、走一遍测试再提交），说一句「帮我优化这段代码」不该变成一整套流程，所以必须由人主动敲命令启动。

改手动的技能要同时做三件事，缺一不可：

1. frontmatter 加 `disable-model-invocation: true`
2. `agents/openai.yaml` 加 `policy.allow_implicit_invocation: false`（两个 harness 要一致）
3. `description` 从模型触发词改写成给人看的一句话，因为它会出现在斜杠命令列表里

## 改完之后

```bash
claude plugin validate . --strict
```

版本号在 `.claude-plugin/plugin.json` 里手动维护。
